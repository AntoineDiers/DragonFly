FROM ubuntu:24.04

# Install npm
ENV NVM_DIR="/usr/local/nvm"
ENV NODE_VERSION="24.11.1"
ENV PATH="/usr/local/nvm/versions/node/v${NODE_VERSION}/bin:${PATH}"
ENV NG_CLI_ANALYTICS="false"
COPY assets/install_npm.sh /tmp/install_npm.sh
RUN /tmp/install_npm.sh

# Install rust 
ENV CARGO_HOME=/home/ubuntu/.cargo
ENV RUSTUP_HOME=/home/ubuntu/.rustup
ENV PATH="${CARGO_HOME}/bin:${PATH}"
COPY assets/install_rust.sh /tmp/install_rust.sh
RUN /tmp/install_rust.sh
RUN chmod -R a+rw $CARGO_HOME

# Install rust dependencies
RUN apt update && apt install -y build-essential libudev-dev pkg-config

# Install sphinx
COPY assets/install_sphinx.sh /tmp/install_sphinx.sh
RUN /tmp/install_sphinx.sh