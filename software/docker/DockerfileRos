FROM ubuntu:24.04

# Install ROS
COPY assets/install_ros.sh /tmp/install_ros.sh
RUN /tmp/install_ros.sh

# Install Firmware
COPY assets/firmware /firmware

# Install entrypoint
COPY assets/entrypoints/ros_entrypoint.sh /entrypoint.sh